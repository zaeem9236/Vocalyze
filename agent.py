import time
def agent(user_phone_num: str, num_questions: str, language: str, status):
    status["call_status"] = 'queue'
    # time.sleep(2)
    # status["call_status"] = 'busy'
    status["call_status"] = 'completed'
    # status["call_status"] = 'failed'
    return {
    
}